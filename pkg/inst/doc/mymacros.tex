%  overall formatting commands
%\magnification=1200 
%\baselineskip 24pt 
%\raggedbottom
%\hsize=6.3truein
%\vsize=9.5 truein

%uppercase font
\font\caps=cmcsc10

%\font\bigbold=TimesB at 12pt

%  macros for bold Greek
%\input bold-greek-math.tex  % for Computer Modern
%\input bold-greek-lucida.tex % for Lucida
%%  (it should be possible to unify these
%%   since they only differ in the first line)


%  other bold math 
\def \bu{{\bf u}}
\def \bU{{\bf U}}
\def \ba{{\bf a}}
\def \bc{{\bf c}}
\def \by{{\bf y}}
\def \bY{{\bf Y}}
\def \bz{{\bf z}}
\def \bx{{\bf x}}
\def \be{{\bf e}}
\def \bX{{\bf X}}
\def \bT{{\bf T}}
%  Roman words that often appear in math
\def\constant{\hbox{constant}}
\def\var{\mathop{\rm var}}
\def\cov{\mathop{\rm cov}}
\def\sd{\mathop{\rm sd}}
\def\corr{\mathop{\rm corr}} 
\def\se{\mathop{\rm se}}
\def\rank{\mathop{\rm rank}} 
\def\rss{\hbox{rss}}
\def\sign{\mathop{\rm sign}}
\def\max{\mathop{\rm max}}
\def\min{\mathop{\rm min}}
\def\logit{\mathop{\rm logit}}
\def\expit{\mathop{\rm expit}}
\def\diag{\mathop{\rm diag}}
\def\trace{\mathop{\rm trace}}
\def\round{\mathop{\rm round}}
\def\Poisson{\mathop{\rm Poisson}}
\def\exponential{\mathop{\rm exponential}}
\def\binomial{\mathop{\rm binomial}}
\def\multinomial{\mathop{\rm multinomial}}
\def\pr{\mathop{\rm pr}}
\def\P{\mathop{\rm P}}
\def\plim{\mathop{\rm plim}}
\def\tendsinprobto{\buildrel\rm
pr\over\longrightarrow}
\def\tendsto{\rightarrow}
\def\implies{\Rightarrow}

\def \dx{\hbox{\hskip 2pt d$x$}}
%  italic expressions in text
\def\ie{\hbox{\it i.e.}}
\def\via{\hbox{{\it via\/}\ }}
\def\eg{{\hbox{\it e.g.}}}
\def\etc{\hbox{\it etc.}}

%  commonly typed sequences in my work
\def\betas{\beta_1,\ldots,\beta_p}
\def\ys{y_1,\ldots,y_n}
\def\half{{1\over 2}}
\def\L{\begingroup\it\char'44\endgroup}
\def\jrssb{{\it JRSS\/}B}
\def\jrssbfull{{\it Journal of the Royal Statistical Society, 
Series B}}
\def\twiddle{{\char'176}}


\def\pounds{{\char'243\hskip 1pt}}
\def\pounds{{\it\$\hskip 1pt}} % for Computer Modern

%\font\marvosym=fmvr8x
%\def\euro{{\marvosym\char164}}

\def\acute{{\char171}}
\def\eacute{{\char142}}

\endinput